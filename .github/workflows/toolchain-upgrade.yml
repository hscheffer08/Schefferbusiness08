name: Controlled toolchain upgrade
on:
  push:
    paths:
      - 'scripts/toolchain-upgrade.trigger'
jobs:
  upgrade:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
      - name: Install current dependencies
        run: npm ci
      - name: Upgrade compatible lint toolchain
        run: npm install --save-dev eslint@^9.39.1 @eslint/js@^9.39.1 typescript-eslint@^8.68.0
      - name: Validate catalog, types and production build
        run: npm run build
      - name: Run lint
        run: npm run lint
      - name: Check remaining vulnerabilities
        run: npm audit --audit-level=high || true
      - name: Commit validated toolchain upgrade
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add package.json package-lock.json
          git commit -m 'Upgrade lint toolchain and reduce dependency risk' || exit 0
          git push
